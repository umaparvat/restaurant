import { connect } from "react-redux";
import Details from "../components/Details.jsx";

const mapStateToProps = state => {};

const DetailsContainer = connect()(Details);

export default DetailsContainer;
